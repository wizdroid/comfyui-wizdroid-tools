# `sets/male/` — Male complete-look sets

Catalogs here appear under category `sets/male` in the **🧙 Preset** node.

No male set catalogs exist yet — drop any future male looks here as JSON
files (same schema as the other catalogs). They show up automatically on a
ComfyUI page refresh.
