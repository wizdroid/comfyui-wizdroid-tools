# `sets/male/` — Male complete-look sets

Each catalog here becomes its own node under `🧙 Wizdroid/Presets/Sets/Male`.

No male set catalogs exist yet — drop any future male looks here as JSON
files (same schema as the other catalogs). They show up as nodes on a
ComfyUI restart.
